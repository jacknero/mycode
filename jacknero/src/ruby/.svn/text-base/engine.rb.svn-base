class CollaborativeFiltering
	def mean(data)
	 t = 0.0;
	 for x in data do
      t += x
	 end
	 return t / data.size
	 end
end

cf = CollaborativeFiltering.new
a = Array.new
a << 1.0
a << 2.0
p a
p cf.mean(a)

p "hoge"

