#include <iostream>
#include <string>
#include <algorithm>
using namespace std;
 
int main()
{
        int a[100002];
        int N,K,i,t;
        string s;
 
        cin >> N;
        for(i=0;i<N;i++) cin >> a[i];
        sort(a, a+N);
        cin >> s >> K;
        while(K--) {
                cin >> t;
                cout << a[t-1] << endl;
        }
 
        return 0;
}

